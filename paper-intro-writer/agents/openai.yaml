interface:
  display_name: "Paper Intro Writer"
  short_description: "Draft intros with ordered citations."
  default_prompt: "Use $paper-intro-writer to draft a concise academic introduction with first-appearance citation ordering."

policy:
  allow_implicit_invocation: true
